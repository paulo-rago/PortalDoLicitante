apiVersion: v1
kind: Service
metadata:
  name: portal-backend
  labels:
    app: portal-backend
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: portal-backend 